<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Không cần quyền hệ thống đặc biệt: mọi lệnh shell chạy qua Shizuku -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.SensiAnalytics"
        android:supportsRtl="true"
        tools:targetApi="34">

        <!-- LoginActivity là màn hình khởi động (LAUNCHER) - yêu cầu nhập key
             kích hoạt trước khi vào MainActivity. Xem LicenseManager.java. -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.SensiAnalytics">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.SensiAnalytics" />

        <!--
            Provider bắt buộc để Shizuku có thể phát hiện app và gửi broadcast
            permission result (dùng cho Shizuku chạy qua ADB, không cần root).
        -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

    </application>

</manifest>
